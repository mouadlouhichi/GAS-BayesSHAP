[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="gas-bayesshap"
version="11.0.0"
requires-python=">=3.9"
dependencies=["numpy>=1.22", "scipy>=1.9", "PyYAML>=6.0"]
[tool.pytest.ini_options]
testpaths=["tests"]
